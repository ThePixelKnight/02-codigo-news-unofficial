import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02-codigo-news-unofficial',
  webDir: 'www'
};

export default config;
